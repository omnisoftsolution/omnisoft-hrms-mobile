<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- Strip permissions auto-injected by plugins (file_picker,
         image_picker, camera, open_filex) that the app does not use.
         Without these removals the merged manifest requests Microphone
         (RECORD_AUDIO) and Phone (READ_PHONE_STATE), which scare users
         on the Play listing and contradict the Data safety declaration,
         and the media permissions trigger Google's Photo & Video
         Permissions policy review.

         Image receipts use image_picker, which uses the Android system
         Photo Picker (no READ_MEDIA_IMAGES needed on API 33+, backported
         below via Play services). Document attachments use file_picker,
         which uses the Storage Access Framework (no media perms needed).
         The app never records audio (camera runs enableAudio: false) or
         picks audio/video. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>

    <!-- Attendance network gate (spec 2026-08-03): reading the connected
         SSID/BSSID needs ACCESS_WIFI_STATE + ACCESS_NETWORK_STATE plus the
         already-granted fine location. Declared here deliberately — see the
         Play Data-safety note below; location is already justified for the
         geofence and the same justification covers Wi-Fi network identity. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>
    <application
        android:label="Omni HR"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Disable Impeller (Vulkan) and use Skia instead. flutter_pdfview
             (AndroidView platform view) renders blank/transparent when
             composited through Impeller on this device. Skia handles
             AndroidView correctly. Revisit if Flutter deprecates Skia or
             we migrate the PDF viewer to a Texture-based renderer (pdfrx). -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>
</manifest>
